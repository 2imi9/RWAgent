# Environmental Research Agent

A FastAPI + LLM application that routes between OpenAI and Claude models and connects with NVIDIA Earth-2 FourCastNet for weather analytics.

## Quick Start
```bash
docker-compose up --build
```
Access the API at `http://localhost:8080/docs`.
# RWAgent
